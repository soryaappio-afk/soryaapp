#############################################
# Environment Example                       #
# Copy to .env and fill in real values.     #
#############################################

## Core
DATABASE_URL="mysql://user:pass@localhost:3306/sorya"
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-5"

## NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-me"

## OAuth (optional – leave blank to hide buttons)
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

## Pusher Realtime
PUSHER_APP_ID=""
PUSHER_KEY=""
PUSHER_SECRET=""
PUSHER_CLUSTER="eu"
NEXT_PUBLIC_PUSHER_KEY=""        # same as PUSHER_KEY
NEXT_PUBLIC_PUSHER_CLUSTER="eu"   # same as PUSHER_CLUSTER

## Notes
# - OPENAI_MODEL kept configurable (default gpt-5)
# - Public (NEXT_PUBLIC_) vars are safe for client exposure
# - Keep secrets (no NEXT_PUBLIC_) out of the client

